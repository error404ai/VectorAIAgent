[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.12.15",
    "base58",
    "browser-use[cli]",
    "patchright>=1.56.0",
    "playwright>=1.57.0",
    "pyinstaller==6.14.1",
    "solana>=0.36.9",
    "solders>=0.26.0",
]

[tool.uv.sources]
browser-use = { path = "browser-use-whiskyba", editable = true }
